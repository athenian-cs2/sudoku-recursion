import java.util.ArrayList;

public class ArrayListPractice {

    // Returns the sum of all values in the list
    public static int combineValues(ArrayList<Integer> list) {
        // REPLACE WITH YOUR CODE
        return 0;
    }

    // Create and return an ArrayList containing the three input Strings.
    public static ArrayList<String> makeList(String first, String second, String third) {
        return null;
    }

    // Given an ArrayList and an array of the same length, create a new ArrayList
    // the has alternating values (the first element of list, then the first element
    // of array, then the second element of list, then the second of array, etc.
    public static ArrayList<Integer> interweave(ArrayList<Integer> list, int[] array) {
        // REPLACE WITH YOUR CODE
        return null;
    }

    public static void main(String[] args) {
        // Write some code here to test your methods!
    }
}